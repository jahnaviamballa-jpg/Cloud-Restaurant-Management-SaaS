version: "3.9"

services:
  backend:
    build:
      context: ../..
      dockerfile: cloud/docker/Dockerfile.backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ../..
      dockerfile: cloud/docker/Dockerfile.frontend
    ports:
      - "5173:5173"